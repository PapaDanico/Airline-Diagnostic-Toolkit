# Test toolchain dependencies (installed in CI / locally, never deployed)
tests/node_modules/
node_modules/

# OS / editor noise
.DS_Store
*.log

# Local Netlify folder
.netlify
tests/.verification/

# Deploy artifacts (built by scripts/package-deploy.mjs)
dist/
